{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C Program",
            "type": "lldb",           
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "preLaunchTask": "build",
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "externalConsole": false
        }
    ]
}